# Trained bundle — 34 MB. Rebuild with:
#   python tbt_model.py train <prepared.csv> --fast
# It is a build artifact: it changes on every monthly retrain, and git stores each
# version in full since binaries do not delta. Use Releases or Git LFS if you need
# it versioned.
*.joblib

# Data — contains customer names, project names and real pricing. Do not commit.
*.csv
!sample_input.csv

# Generated outputs
audit*.xlsx
scored*.xlsx
*_prepared.csv
*.log

__pycache__/
*.py[cod]
.venv/
venv/
.DS_Store

# Built model bundles and run outputs
models/
build_log.json
RESULTS.md
report*.txt
increments.log
